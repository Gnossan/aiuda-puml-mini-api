## Aiuda - PlantUML Renderer

### Teknikstack

**Teknikstack är redan beslutad — använd exakt detta, inga egna val:**
- React 18 + Vite + **TypeScript**
- npm, inga externa state-bibliotek (bara `useState`)

### Hur man kör lokalt

```bash
npm install
npm run dev
```